# 2026-07-22 工作日志

## 网站未自动更新修复

**问题**：新的一天网站开奖数据未更新（停在 26191 期 2026-07-20）。

**根因**：`data sync` 和 `ops run-once` 无人手动/自动执行。前端 5 分钟自动刷新只重新拉 API，数据库不更新 API 内容不会变。

**修复**：
1. 运行 `data sync --limit 60` → 新增 26192 期（2026-07-21, 3,0,6），来源声明 7666→7667。
2. 运行 `ops run-once` → 生成新预测（`p3-2026-07-22T01-59-59-048Z`）和 2026-07-22 日报。
3. 重启 serve-reports 使服务加载新数据。

**新增功能**：
- 后端 `/api/v1/overview` 增加 `data.latestDraw` 字段，返回最新开奖完整详情（d1/d2/d3/numbers）。
- 前端 `OverviewPage` draw-hero 区优先从 `latestDraw` 渲染号码球，fallback 到 settlement。
- 创建**每日自动同步**自动化：每天 9:30 自动跑 `data sync` + `ops run-once`。

**改动文件**：
- `packages/cli/src/ops.ts`：overview 端点补 latestDraw SQL 查询。
- `packages/web/src/main.tsx`：OverviewData 接口加 latestDraw 字段；OverviewPage 优先取 latestDraw 展示号码。
- 自动化：`~/.workbuddy/workbuddy.db` automations 表新增每日同步任务。

**服务状态**：运行于 127.0.0.1:4318（后台 task DQ88Vr）。

## 走势分析页（路数偏态可视化）

**背景**：用户指出"下期预测结果一直没更新"（模型加权频率输出固定票 415/145/514，不捕捉近期路数偏态）。分析了 069、036 两期均为全 0 路、共用 0/6，模型票在历史 7667 期中从未出现。用户要求"优化改进"。

**新增功能（T20–T23）**：
- 后端 `packages/cli/src/ops.ts`：新增 `GET /api/v1/draws?limit=N`（默认 600，上限 2000），直接查 `draws` 表返回近期开奖（period/period_num/draw_date/d1/d2/d3/numbers/drawStatus），倒序。
- 前端 `packages/web/src/main.tsx`：导航加"走势分析"（LineChartIcon）；新增 `TrendsAnalysisPage`，从 `/api/v1/draws` 取数纯前端计算：
  - 窗口切换（50/100/200 期）
  - 洞察卡：连续全 0 路期数、最近两期共用数字、最活跃/最冷数字
  - 012路占比堆叠柱（百/十/个 × 0/1/2路）
  - 012路走势折线（近 50 期）
  - 号码活跃度热力图（3 位 × 10 数字，VisualMapComponent+HeatmapChart）
  - 和值走势折线（近 100 期）
- `packages/web/src/styles.css`：补 `.analysis-bar/.chip-btn/.insight-row/.insight-card/.muted-text` 等样式（原 `.muted-text` 缺失，一并补上）。
- echarts 导入补 `HeatmapChart` + `VisualMapComponent`。

**改动文件**：`packages/cli/src/ops.ts`、`packages/web/src/main.tsx`、`packages/web/src/styles.css`。

**验证**：cli tsc 编译通过；vite build 成功；serve 重启后 `/api/v1/draws?limit=5` 返回 count=5、倒序正确、含最新 26192；前端 JS 含「走势分析/012路占比/连续全 0 路」；vitest 10/10 通过。

**服务状态**：运行于 127.0.0.1:4318（后台 task JI6gCg）。

**说明**：此页是"偏态可视化"，让研究台能看到模型未捕捉的路数/集群特征；并未改变预测模型本身（彩票本质随机，模型调优属另项工作）。

